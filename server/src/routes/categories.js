import { Router } from 'express'; import Category from '../models/Category.js'; import { protect } from '../middleware/auth.js';
const router=Router();router.get('/',async(req,res,next)=>{try{res.json(await Category.find({active:true}).sort({name:1}))}catch(e){next(e)}});router.post('/',protect,async(req,res,next)=>{try{const slug=req.body.name.toLowerCase().replace(/[^a-z0-9]+/g,'-');res.status(201).json(await Category.create({...req.body,slug}))}catch(e){next(e)}});router.put('/:id',protect,async(req,res,next)=>{try{res.json(await Category.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true}))}catch(e){next(e)}});router.delete('/:id',protect,async(req,res,next)=>{try{await Category.findByIdAndDelete(req.params.id);res.status(204).end()}catch(e){next(e)}});export default router;
