{
  "name": "rahim-fabrics",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "install:all": "npm install"
  }
}
